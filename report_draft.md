# 検証レポート: 立ち座り姿勢判定アルゴリズムの開発

---

## 概要

野外ロックフェスティバルにおける観客の盛り上がり状況を定量化するため、人物画像から「立っているか・座っているか」を判定するアルゴリズムを開発した。COCO形式の姿勢推定データ（17キーポイント）とバウンディングボックス情報を入力とし、幾何学的特徴量を用いた分類器を構築した。

検証は以下の段階で実施した（v4以降は修正データセット803件を使用）：

| バージョン | 手法 | Macro F1 | 前版比 |
|---|---|---|---|
| v1 | ルールベース（スコアリング, 5特徴量）| 0.7172 | baseline |
| v2 | ルールベース改良（バグ修正 + 8特徴量）| 0.7300 | +0.013 |
| v3 | LightGBM + StratifiedKFold(5)（22特徴量）| 0.7915 | +0.062 |
| **v4** | **LightGBM + 生座標追加（40特徴量, num_leaves=20）**| **0.8585** | — ※ |
| v5 | LightGBM + colsample_bynode=0.7（40特徴量）| 0.8558 | -0.003 vs v4 |

※ v4・v5は修正データセット（803件）で再学習。v1-v3（旧927件）との直接比較不可。

最終モデル（v4）は修正データセットでOOF Macro F1 = **0.8585** を達成した。下半身6キーポイントの生座標（bbox正規化）を直接追加したことで、手作り特徴量では捉えられなかったパターンをモデルが自力で学習した。`raw_right_hip_x`（重要度2位）・`raw_left_hip_x`（4位）が高重要度となり、腰のx座標が姿勢判別において未活用の情報であることが再確認された。

今後の優先課題は閾値最適化・他モデル（XGBoost, CatBoost）との比較検証・遮蔽パターンの明示的特徴量化である。

---

# 1. はじめに

## 1.1 背景

野外ロックフェスティバルを主催するAさんは、最近出演してくれているバンドメンバーから「演奏に夢中で、お客さんが盛り上がっているかがいまいち分からない」という相談を受けている。お客さんが盛り上がっているかどうかが分かればそれによってMCを変えたり、場合によっては曲順を変えることでステージをより良いものにできるというのだ。Aさん自身も、例えば盛り上がり過ぎている時に警備員を多めに派遣するなど、安全管理の面からもお客さんの盛り上がり状況をモニタリングすることは大切と考えた。

早速お客さんの盛り上がりを定量化する方法を考えて、**画像認識によって「立って聞いているお客さんの割合」を調べる**ことができればそのステージの盛り上がり具合が見積もれることに気がついた。

Aさんの友人であるあなたはAさんを助けるために、**人の画像からその人が立っているか座っているかを判定するAI**を作成してあげることにした。

## 1.2 目的

**野外ロックフェスティバルにおけるバンドの盛り上がり方を評価する指標として、「立って聞いているお客さんの割合」を算出するために、人の画像からその人が立っているか座っているかを判定するアルゴリズムを開発する**

## 1.3 アプローチ→　要修正

野外ロックフェスティバルで撮影した画像に物体検出と姿勢推定を適用し、人物のBounding Boxと姿勢を抽出し、人物が立っているか座っているかを判定する。

## 1.4 データ

v1〜v3の検証には旧データセット（927件）を使用した。その後、データセットの品質修正（ノイズ除去・アノテーション精度向上）が実施され、v4以降の検証には修正データセット（803件）を使用している。

| データセット | sitting | standing | 合計 |
|---|---|---|---|
| 旧（v1-v3用） | 440 | 487 | 927 |
| 修正後（v4-v5用） | 376 | 427 | 803 |

いずれのデータセットも、クラス比は約47〜48% / 52〜53%で実質的な不均衡はない。各サンプルはCOCO形式の17キーポイント座標・信頼度スコアとバウンディングボックス情報で構成される（108次元）。

---

# 2. 実験結果

## 2.1 v1 — ルールベース（スコアリング方式, 5特徴量）

### 仮説

姿勢の違いは以下の幾何学的特徴に最も顕著に現れると考えた：

1. **bboxアスペクト比（h/w）**: 立っている人物は縦長（h/w ≈ 1.8）、座っている人物は比較的正方形（h/w ≈ 1.3）になる
2. **腰-膝の垂直距離**: 立ち姿では膝が腰より大きく下方に位置するため、この距離が長くなる
3. **肩-腰-膝角度**: 立ち姿では体幹が伸び、角度が大きくなる（~145°）

これらの特徴量に閾値を設定し、スコアリングによって立ち座りを判別できると仮説を立てた。

### ロジック

EDAで算出したCohen's d上位の5特徴量（bbox_aspect_ratio、左右のhip-knee垂直距離、肩-腰-膝角度）を用い、各特徴量について「standingらしさ」スコア（+1/-1）を付与。合計スコアが正の場合をstandingと判定するスコアリング方式を実装した。

| 特徴量 | Cohen's d | 判定閾値 |
|---|---|---|
| right_hip_knee_vert_dist | 1.17 | > 0.12 → standing |
| left_hip_knee_vert_dist | 1.16 | > 0.12 → standing |
| bbox_aspect_ratio | 0.86 | > 1.55 → standing |
| angle_left_shoulder_hip_knee | 0.85 | > 128° → standing |
| angle_right_shoulder_hip_knee | 0.80 | > 128° → standing |

### 適用結果

**定量評価**

| 指標 | 値 |
|---|---|
| Macro F1 | 0.7172 |
| Sitting Precision/Recall | 0.73 / 0.65 |
| Standing Precision/Recall | 0.71 / 0.78 |

**定性評価**

- sittingの見逃し（sittingをstandingと誤判定）が多い
- bboxアスペクト比の条件分岐にif/elifの記述順バグが存在し、一部のケースで正しく評価されていなかった

### 考察

Cohen's dが最高の特徴量を使った単純なスコアリングでもMacro F1 = 0.72程度を達成できた。一方で、**各特徴量は独立に閾値判定されており、特徴量間の相互作用（例：角度が低くてもbboxが縦長なら高確率でstanding）を捉えられていない**。また、実装バグが発見されたため修正が必要と判断した。

---

## 2.2 v2 — ルールベース改良（バグ修正 + 8特徴量）

### 仮説

v1のバグを修正しつつ、下半身キーポイントの信頼度情報と腰のy座標を追加することで、**キーポイントが隠れているケース（遮蔽）を識別**できると仮説を立てた。座り姿では椅子や机でひざ・足首が隠れるため、信頼度が低いこと自体が「sitting」の証拠になりうる。

### ロジック

v1に以下を追加した：

- バグ修正: bbox_aspect_ratioの条件分岐を正しい順序に修正
- 追加特徴量3点:
  - `mean_conf_lower`: 下半身6キーポイント（腰・膝・足首）の平均信頼度（< 0.45 → sittingスコア+1）
  - `rel_left_hip_y`, `rel_right_hip_y`: bbox内の腰のy相対座標（> 0.75 → sittingスコア+1 / < 0.65 → standingスコア+1）

### 適用結果

**定量評価**

| 指標 | 値 |
|---|---|
| Macro F1 | 0.7300 |
| Sitting Precision/Recall | 0.75 / 0.67 |
| Standing Precision/Recall | 0.72 / 0.79 |

**定性評価**

- v1バグ修正により全体的に安定
- standing → sittingの誤分類（特にstanding姿勢でhipが下に位置するケース）が依然として多い
- 信頼度特徴量の効果は限定的（sitting/standingともに平均信頼度が近い: 0.582 vs 0.595）

### 考察

バグ修正と特徴量追加で+1.3ポイントの改善を確認した。しかし**信頼度特徴量単独の識別力は弱く、他の特徴量との組み合わせが必要**であることが分かった。また、ルールベースでは複数の特徴量の非線形な相互作用（例：「角度は低いが信頼度も低い場合はsitting」）を表現できない限界が明確になった。機械学習モデルへの移行が必要と判断した。

---

## 2.3 v3 — LightGBM + StratifiedKFold(5)（22特徴量）

### 仮説

ルールベースで設計した特徴量をそのまま入力とし、勾配ブースティング（LightGBM）で学習させることで、**特徴量間の非線形相互作用**を自動的に捉えられると仮説を立てた。

また、StratifiedKFoldによる5分割交差検証を用いることで、データが927件と限られる中でも信頼性の高い汎化性能の推定ができると考えた。

### ロジック

**特徴量（22次元）**

| カテゴリ | 特徴量名 | 説明 |
|---|---|---|
| Bbox形状 | `bbox_aspect_ratio` | h/w比（立ち=縦長） |
| hip-knee距離 | `left/right_hip_knee_vert` | 膝-腰垂直距離（bbox正規化） |
| hip-ankle距離 | `left/right_hip_ankle_vert` | 足首-腰垂直距離（bbox正規化） |
| 関節角度 | `angle_l/r_shoulder_hip_knee` | 肩-腰-膝角度（立ち≈145°, 座り≈112°） |
| 関節角度 | `angle_l/r_hip_knee_ankle` | 腰-膝-足首角度（立ち≈129°, 座り≈102°） |
| 腰y座標 | `rel_left/right_hip_y` | 腰のbbox内相対y座標 |
| 膝y座標 | `rel_left/right_knee_y` | 膝のbbox内相対y座標 |
| 下半身信頼度 | `mean/min_conf_lower` | 下半身6KPの平均・最小信頼度 
| 個別信頼度 | `conf_left/right_knee/ankle` | 膝・足首個別信頼度 |
| 身体比率 | `torso_leg_ratio` | 体幹長/脚長比率 |
| 膝x位置 | `left/right_knee_x_offset` | 膝の腰に対するx方向オフセット（座り時は前方に出る） |

**モデル設定**

```python
LightGBMClassifier(
    n_estimators=300, learning_rate=0.05,
    num_leaves=31, max_depth=6,
    subsample=0.8, colsample_bytree=0.8,
    reg_alpha=0.1, reg_lambda=0.1, seed=42
)
StratifiedKFold(n_splits=5, shuffle=True, random_state=42)
```

### 適用結果

**定量評価**

OOF（Out-of-Fold）による評価結果：

| 指標 | 値 |
|---|---|
| **OOF Macro F1** | **0.7915** |
| Fold間標準偏差 | 0.022（安定） |

| Fold | Macro F1 |
|---|---|
| 0 | ~0.77 |
| 1 | ~0.81 |
| 2 | ~0.79 |
| 3 | ~0.80 |
| 4 | ~0.80 |

**特徴量重要度（gain）上位10**

| 順位 | 特徴量 | Gain |
|---|---|---|
| 1 | `bbox_aspect_ratio` | 511 |
| 2 | `right_knee_x_offset` | 370 |
| 3 | `angle_r_shoulder_hip_knee` | 330 |
| 4 | `left_hip_knee_vert` | ~280 |
| 5 | `angle_l_hip_knee_ankle` | ~250 |

**定性評価**

- sittingとstandingの双方に対してバランスよく機能
- 下半身キーポイントが低信頼度のサンプル（62.6%）に対しても、bbox_aspect_ratioと膝x-offsetが補完的に機能することで精度を維持
- 5-foldアンサンブルによる推論を `sitting_prediction(data)` 関数として実装済み

### 考察

LightGBMはルールベース（v2: 0.73）を6ポイント上回るMacro F1 = 0.7915を達成した。**特徴量重要度の分析から、EDAでCohen's dが最高だったhip-knee垂直距離は角度特徴量との共線性により重要度が中位に落ちており、モデルが特徴量の冗長性を自動的に処理していることが分かった**。

一方で、**下限となりうる誤分類パターン**として以下が想定される：
- 下半身がすべて遮蔽されたsittingサンプル（bbox_aspect_ratioだけに頼るケース）
- 片足を伸ばして座るなど非定型姿勢

---

## 2.4 v4 — LightGBM + 生座標追加（40特徴量, num_leaves=20）

### 仮説

v3の22特徴量はすべて「設計者が定義した幾何学的特徴」であり、非定型姿勢（片足伸ばし座り、足を組む等）では特徴量計算が崩れる可能性がある。下半身6キーポイント（インデックス11-16: 腰・膝・足首）のbbox正規化座標を直接入力することで、**モデルが自力で判別パターンを発見できる**と仮説を立てた。

### ロジック

**追加特徴量（+18次元）**

各キーポイント（left_hip, right_hip, left_knee, right_knee, left_ankle, right_ankle）について以下の3特徴量を追加：

| 特徴量名 | 説明 | NaN処理 |
|---|---|---|
| `raw_{name}_x` | bbox正規化x座標 (=(kp_x - bbox_x) / bbox_w) | conf < 0.3 → NaN |
| `raw_{name}_y` | bbox正規化y座標 (=(kp_y - bbox_y) / bbox_h) | conf < 0.3 → NaN |
| `raw_{name}_conf` | 信頼度スコア（常に有効）| なし |

信頼度0.3未満のキーポイント座標はNaNとしてLightGBMの欠損処理に委ねる。LightGBMはNaNを自動的に欠損として扱い、splitの欠損方向を学習する。

**モデル設定**

```python
LightGBMClassifier(
    n_estimators=300, learning_rate=0.05,
    num_leaves=20,  # 40次元への増加に伴いoverfitting対策で31→20
    max_depth=6, subsample=0.8, colsample_bytree=0.8,
    reg_alpha=0.1, reg_lambda=0.1, seed=42
)
StratifiedKFold(n_splits=5, shuffle=True, random_state=42)
```

### 適用結果

**定量評価**

OOF（Out-of-Fold）による評価結果（修正データセット 803件）：

| 指標 | 値 |
|---|---|
| **OOF Macro F1** | **0.8585** |
| Fold間標準偏差 | 0.0099（非常に安定） |

| Fold | Macro F1 |
|---|---|
| 0 | 0.8625 |
| 1 | 0.8445 |
| 2 | 0.8691 |
| 3 | 0.8673 |
| 4 | 0.8488 |

**特徴量重要度（gain）上位10**

| 順位 | 特徴量 | Gain |
|---|---|---|
| 1 | `bbox_aspect_ratio` | 422.4 |
| 2 | `raw_right_hip_x` | 297.4 |
| 3 | `left_hip_knee_vert` | 289.8 |
| 4 | `raw_left_hip_x` | 262.0 |
| 5 | `right_knee_x_offset` | 240.2 |
| 6 | `left_knee_x_offset` | 214.2 |
| 7 | `angle_r_hip_knee_ankle` | 212.0 |
| 8 | `angle_l_hip_knee_ankle` | 211.6 |
| 9 | `angle_r_shoulder_hip_knee` | 180.4 |
| 10 | `raw_left_hip_conf` | 173.6 |

**定性評価**

- `raw_right_hip_x`（2位）・`raw_left_hip_x`（4位）が高重要度：腰のx座標は手作り特徴量では直接使われておらず、未活用の情報だったことが改めて確認された
- `raw_left_hip_conf`（10位）が上位に登場：腰キーポイントの信頼度が識別に寄与（遮蔽状況の指標として機能している可能性）
- 足首のx座標（`raw_*_ankle_x`）は依然として低重要度（34-35位）

### 考察

修正データセット（803件）で生座標追加モデルのOOF Macro F1 = **0.8585** を達成した。旧データセット結果（0.8041）より高い値を示しているが、**データセットの修正（ノイズ除去・アノテーション精度向上）が精度向上に大きく寄与**していると考えられるため、v3との単純比較は困難である。

腰のx座標（`raw_hip_x`）が2・4位に入るという知見は新データセットでも再現されており、この特徴の重要性は頑健であることが確認された。

---

## 2.5 v5 — LightGBM + colsample_bynode=0.7（40特徴量, per-node sampling）

### 仮説

v4の `colsample_bytree=0.8`（per-tree）を per-node sampling に切り替えることで、重要特徴量の選択機会が増加すると仮説を立てた。per-tree samplingでは除外された特徴がツリー全体で使われないのに対し、per-node samplingでは各ノードで独立にサンプリングするため、重要特徴が1ノードで外れても別ノードで選ばれる機会がある。

### ロジック

**変更パラメータ（v4との差分のみ）**

| パラメータ | v4（expA01） | v5（expA02） |
|---|---|---|
| `colsample_bytree` | 0.8 | **1.0**（無効化） |
| `colsample_bynode` | なし（デフォルト1.0） | **0.7**（40特徴 × 70% ≈ 28特徴/split） |
| その他 | 同一 | 同一 |

特徴量・fold設計・その他ハイパーパラメータはv4と完全に同一。

### 適用結果

**定量評価**

修正データセット（803件）での比較結果：

| 指標 | v4（expA01） | v5（expA02） | 差分 |
|---|---|---|---|
| OOF Macro F1 | **0.8585** | 0.8558 | -0.003 |
| Fold間標準偏差 | 0.0099 | 0.0078 | v5の方が若干安定 |

| Fold | v4 F1 | v5 F1 |
|---|---|---|
| 0 | 0.8625 | 0.8561 |
| 1 | 0.8445 | 0.8445 |
| 2 | 0.8691 | 0.8688 |
| 3 | 0.8673 | 0.8542 |
| 4 | 0.8488 | 0.8550 |

**特徴量重要度（gain）上位5（v4との比較）**

| 順位 | 特徴量 | v5 Gain | v4 Gain | 変化 |
|---|---|---|---|---|
| 1 | `bbox_aspect_ratio` | 428.6 | 422.4 | ほぼ同 |
| 2 | `left_hip_knee_vert` | 285.2 | 289.8 | ほぼ同 |
| 3 | `raw_right_hip_x` | 276.6 | 297.4 | -20.8 |
| 4 | `raw_left_hip_x` | 265.6 | 262.0 | ほぼ同 |
| 5 | `right_knee_x_offset` | 242.0 | 240.2 | ほぼ同 |

### 考察

修正データセットでもper-node samplingはv4比でOOF F1が **-0.003** 低下した。旧・新データセット双方でv4（per-tree）> v5（per-node）という順位は一貫している。

ただし、新データセットではFold間標準偏差がv5（0.0078）< v4（0.0099）となり、旧データセットで見られた「v5の不安定化（fold1が0.86と突出）」は解消されている。データセット修正によりデータ品質が向上し、per-node samplingの不安定要因が緩和された可能性がある。

**結論**: v4（per-tree, colsample_bytree=0.8）が引き続き最良モデル。per-node samplingは安定性は改善したがスコアでは及ばない。

---

## 2.6 データセット修正後の再学習結果比較（v4・v5）

### 背景

v4・v5の初回学習後にデータセットの品質修正が実施されたため、両モデルを修正データセット（803件）で再学習し、初回結果（旧927件）との比較を行った。

### 定量比較

**v4（expA01: 生座標追加, 40特徴量）**

| 指標 | 旧データセット（927件） | 修正データセット（803件） | 変化 |
|---|---|---|---|
| OOF Macro F1 | 0.8041 | **0.8585** | +0.054 |
| Fold std | 0.020 | 0.0099 | -0.010（安定化） |

| Fold | 旧 | 修正後 |
|---|---|---|
| 0 | 0.7945 | 0.8625 |
| 1 | 0.8433 | 0.8445 |
| 2 | 0.7994 | 0.8691 |
| 3 | 0.7880 | 0.8673 |
| 4 | 0.7945 | 0.8488 |

**v5（expA02: colsample_bynode=0.7, 40特徴量）**

| 指標 | 旧データセット（927件） | 修正データセット（803件） | 変化 |
|---|---|---|---|
| OOF Macro F1 | 0.8009 | **0.8558** | +0.055 |
| Fold std | 0.030 | 0.0078 | -0.022（大幅安定化） |

| Fold | 旧 | 修正後 |
|---|---|---|
| 0 | 0.7889 | 0.8561 |
| 1 | **0.8598** | 0.8445 |
| 2 | 0.7726 | 0.8688 |
| 3 | 0.7880 | 0.8542 |
| 4 | 0.7944 | 0.8550 |

**v4 vs v5 比較（修正データセット）**

| 指標 | v4（案1） | v5（案2） | 差分 |
|---|---|---|---|
| OOF Macro F1 | **0.8585** | 0.8558 | v4 +0.003 |
| Fold std | 0.0099 | 0.0078 | v5がやや安定 |
| sitting Precision/Recall | 0.86 / 0.84 | 0.86 / 0.83 | v4がRecall +0.01 |
| standing Precision/Recall | 0.86 / 0.88 | 0.85 / 0.88 | v4がPrec +0.01 |

### 考察

**スコア向上について**

修正データセットでは両モデルともOOF F1が約+0.054〜0.055向上した。向上の主因として以下が考えられる：
1. **ノイズサンプルの除去**: 旧データセットから誤ラベル・判別困難なサンプルが除外された（927→803件）
2. **アノテーション精度向上**: キーポイント座標やBBoxの精度が改善され、特徴量計算の信頼性が向上した

**v5の安定化**

旧データセットではv5のFold stdが0.030と大きく、Fold1（0.8598）が突出する不安定さが問題だった。修正データセットではstdが0.0078まで改善し、全foldが0.844〜0.869の範囲に収束した。これはデータ修正によるノイズ低減がper-node samplingの不安定要因を除去したためと考えられる。

**v4 > v5 の結論の頑健性**

旧・新データセットの双方でv4がv5を+0.003上回るという結果は一貫しており、この結論はデータセットに依存しない頑健な知見である。**per-tree sampling（v4）がper-node sampling（v5）より本タスクに適している**という判断は変わらない。

---

## 2.7 データ分析: 低信頼度キーポイントの実態（ハルシネーション座標）

v4・v5の実験を通じて下半身キーポイントの低信頼度が精度上限となっていることが示唆されたため、低信頼度時のキーポイント座標の実態を調査した。

### 調査結果

信頼度 < 0.1 のキーポイント（膝・足首）を対象に、座標値の実態を確認した。

| 項目 | 結果 |
|---|---|
| 対象KP数（sitting 440件、膝/足首4KP） | 594件 |
| 座標が (0, 0) | **0件（0%）** |
| bbox範囲外の座標 | **392件（66%）** |

代表的なケース：

```
kp15 conf=0.012  coords=(706.2, 720.0)  bbox範囲: y=74〜715
kp15 conf=0.064  coords=(628.5, 720.0)  bbox範囲: y=32〜714
kp16 conf=0.065  coords=(645.3, 720.0)  bbox範囲: y=32〜714
```

**y = 720.0（画像下端）に座標が固定されているケースが多数確認された。** 姿勢推定モデル（ViTPose等）は下半身が画像フレーム外にある場合、nullではなく画像端ピクセルに座標を押し込む挙動をとる。すなわち、低信頼度KPの座標は「欠損」ではなく「ハルシネーション（幻の座標）」である。

### 現行モデルへの影響

| 特徴量グループ | ハルシネーション座標の影響 |
|---|---|
| `raw_{name}_x/y`（v4追加分） | conf < 0.3 で NaN 化済み ✅ **対処済み** |
| `angle_*`、`hip_knee_vert` 等、既存22特徴量 | **bbox範囲外の偽座標をそのまま使用** ⚠️ |

例として、下半身不可視のsittingサンプルでは `angle_l_hip_knee_ankle` の計算に y=720（bbox下端を大幅に超えた値）が入り込み、角度が異常値（例: 170°超）になる。これがモデルの判別精度を下げている一因と考えられる。

### 示唆される改善

既存22特徴量の計算においても、**conf < 0.3 のキーポイントを使う場合に NaN または安全なフォールバック値（例: 平均値代入）を適用する**ことで、ハルシネーション座標の影響を排除できる。

```python
# 現行（問題あり）
features["angle_l_hip_knee_ankle"] = _angle_between(
    kps[LEFT_HIP], kps[LEFT_KNEE], kps[LEFT_ANKLE]
)

# 改善案: 信頼度が低い場合は NaN
if scores[LEFT_KNEE] >= 0.3 and scores[LEFT_ANKLE] >= 0.3:
    features["angle_l_hip_knee_ankle"] = _angle_between(...)
else:
    features["angle_l_hip_knee_ankle"] = float("nan")
```

この修正はコスト低で、v4ベースに適用した場合 **+0.01〜0.02 程度の改善**が見込まれる。また、F1 = 0.9 達成には画像そのものをCNNで処理するアプローチが必要と考えられるが、本修正はキーポイントベースの手法での上限を高める最優先の改善策である。

---

# 3. 結論

本検証では、姿勢推定による幾何学的特徴量を用いた立ち座り判定アルゴリズムを段階的に開発した。

**全バージョン比較**

| バージョン | 手法の要点 | OOF Macro F1 | データセット |
|---|---|---|---|
| v1 | ルールベース 5特徴量 | 0.7172 | 旧 927件 |
| v2 | ルールベース改良 8特徴量 | 0.7300 | 旧 927件 |
| v3 | LightGBM 22特徴量 | 0.7915 | 旧 927件 |
| **v4** | **LightGBM + 生座標追加 40特徴量** | **0.8585（最良）** | 新 803件 |
| v5 | LightGBM + colsample_bynode 40特徴量 | 0.8558 | 新 803件 |

**主な成果**

- v4（生座標追加）が最良モデル：OOF Macro F1 = **0.8585**（修正データセット）
- **新発見**: 腰のx座標（`raw_hip_x`）が重要度2・4位。新旧両データセットで一貫して高重要度
- **v5の知見**: per-node samplingは安定性改善（std 0.030→0.0078）したが、スコアでは v4に0.003及ばず。per-tree（v4）が依然最良
- v4 > v5 という順位は旧・新データセット双方で一貫（頑健な結論）
- データセット修正による品質向上が大きな精度改善に寄与（v3→v4比較は同一データでないため注意）

**次ステップ提案（v4・v5の両結果を踏まえた判断）**

v4 > v5 という結果から、**HPよりも特徴量設計が効く**というシグナルが得られた。次の打ち手はこの方針に沿ったものが有効。

```
堅実案0（最優先・低コスト）: 既存22特徴量のハルシネーション座標除去（expA03）
  → conf<0.3 のKPを使う angle/distance 計算に NaN フォールバックを適用。
  → データ調査で低信頼度KPの66%がbbox外の偽座標（y=720固定等）と判明。
  → v4ベースへの適用で +0.01〜0.02 の改善見込み。実装1〜2時間。

堅実案1: 全17KP生座標の追加（expA04）
  → v4では下半身6KP（11-16）のみ追加。上半身KP（肩・肘・手首: 5-10）
    の生座標をさらに追加（+18次元、計58次元）。
  → raw_hip_xが2・4位に入ったことから、上半身の水平位置も有効な可能性。
  → 期待F1改善: +0.005〜0.010

堅実案2: 閾値最適化
  → v4モデルの確率出力に対してPrecision/Recall曲線で最適閾値を算出。
  → 現在の0.5をsitting-recallとstanding-recallのバランスに応じて調整。
  → 実装コスト低、v4の学習済みモデルをそのまま使用可能。

爆発案（高リスク・高リターン）: v4 + v5 アンサンブル
  → 同じ特徴量・異なるサンプリング戦略の2モデルを確率平均でアンサンブル。
  → 相関が高いため効果は限定的だが、fold間の差異を補完できる可能性。
  → 実装コスト低（predict時に10モデル平均に変更するだけ）。
```

**残課題**

1. **ハルシネーション座標の除去（最優先）**: 低信頼度（conf<0.3）KPを使う既存22特徴量の angle/distance 計算に NaN フォールバックを適用。bbox外の偽座標（66%が画像端に固定）による特徴量汚染を除去する
2. **特徴量拡張**: 上半身KP生座標（5-10）の追加
3. **閾値最適化**: 固定0.5の見直し
4. **モデル比較**: XGBoost・CatBoostとの比較未実施

---

# 4. 参考文献

1. COCO Keypoints format: [https://cocodataset.org/#keypoints-2020](https://cocodataset.org/#keypoints-2020)
2. LightGBM: Ke, G. et al. (2017). "LightGBM: A Highly Efficient Gradient Boosting Decision Tree." *NeurIPS 2017*.
3. Cohen, J. (1988). *Statistical Power Analysis for the Behavioral Sciences* (2nd ed.). — Cohen's d の参照基準（d > 0.8: large effect size）
4. scikit-learn: Pedregosa et al. (2011). "Scikit-learn: Machine Learning in Python." *JMLR 12*, 2825–2830.
