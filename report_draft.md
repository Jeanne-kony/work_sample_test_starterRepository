# 検証レポート: 立ち座り姿勢判定アルゴリズムの開発

---

## 概要

野外ロックフェスティバルにおける観客の盛り上がり状況を定量化するため、人物画像から「立っているか・座っているか」を判定するアルゴリズムを開発した。COCO形式の姿勢推定データ（17キーポイント）とバウンディングボックス情報を入力とし、幾何学的特徴量を用いた分類器を構築した。

検証は以下の3段階で実施した：

| バージョン | 手法 | Macro F1 | 前版比 |
|---|---|---|---|
| v1 | ルールベース（スコアリング, 5特徴量）| 0.7172 | baseline |
| v2 | ルールベース改良（バグ修正 + 8特徴量）| 0.7300 | +0.013 |
| v3 | LightGBM + StratifiedKFold(5)（22特徴量）| **0.7915** | +0.062 |

最終モデル（v3）はOOF Macro F1 = **0.7915** を達成した。最も重要な特徴量はbboxのアスペクト比（h/w）であり、次いで膝の水平オフセット、肩-腰-膝角度が続く。下半身キーポイントの信頼度が低いサンプルが62.6%存在するという課題が残るが、信頼度情報を特徴量として明示的に組み込むことで部分的に対処した。

今後の優先課題は閾値最適化・特徴量拡張（全17キーポイント相対座標）・他モデル（XGBoost, CatBoost）との比較検証である。

---

# 1. はじめに

## 1.1 背景

野外ロックフェスティバルを主催するAさんは、最近出演してくれているバンドメンバーから「演奏に夢中で、お客さんが盛り上がっているかがいまいち分からない」という相談を受けている。お客さんが盛り上がっているかどうかが分かればそれによってMCを変えたり、場合によっては曲順を変えることでステージをより良いものにできるというのだ。Aさん自身も、例えば盛り上がり過ぎている時に警備員を多めに派遣するなど、安全管理の面からもお客さんの盛り上がり状況をモニタリングすることは大切と考えた。

早速お客さんの盛り上がりを定量化する方法を考えて、**画像認識によって「立って聞いているお客さんの割合」を調べる**ことができればそのステージの盛り上がり具合が見積もれることに気がついた。

Aさんの友人であるあなたはAさんを助けるために、**人の画像からその人が立っているか座っているかを判定するAI**を作成してあげることにした。

## 1.2 目的

**野外ロックフェスティバルにおけるバンドの盛り上がり方を評価する指標として、「立って聞いているお客さんの割合」を算出するために、人の画像からその人が立っているか座っているかを判定するアルゴリズムを開発する**

## 1.3 アプローチ→　要修正

野外ロックフェスティバルで撮影した画像に物体検出と姿勢推定を適用し、人物のBounding Boxと姿勢を抽出し、人物が立っているか座っているかを判定する。

## 1.4 データ

927サンプル（sitting: 440件、standing: 487件）を使用した。クラス比は約47.5% / 52.5%で、実質的な不均衡はない。各サンプルはCOCO形式の17キーポイント座標・信頼度スコアとバウンディングボックス情報で構成される（108次元）。

---

# 2. 実験結果

## 2.1 v1 — ルールベース（スコアリング方式, 5特徴量）

### 仮説

姿勢の違いは以下の幾何学的特徴に最も顕著に現れると考えた：

1. **bboxアスペクト比（h/w）**: 立っている人物は縦長（h/w ≈ 1.8）、座っている人物は比較的正方形（h/w ≈ 1.3）になる
2. **腰-膝の垂直距離**: 立ち姿では膝が腰より大きく下方に位置するため、この距離が長くなる
3. **肩-腰-膝角度**: 立ち姿では体幹が伸び、角度が大きくなる（~145°）

これらの特徴量に閾値を設定し、スコアリングによって立ち座りを判別できると仮説を立てた。

### ロジック

EDAで算出したCohen's d上位の5特徴量（bbox_aspect_ratio、左右のhip-knee垂直距離、肩-腰-膝角度）を用い、各特徴量について「standingらしさ」スコア（+1/-1）を付与。合計スコアが正の場合をstandingと判定するスコアリング方式を実装した。

| 特徴量 | Cohen's d | 判定閾値 |
|---|---|---|
| right_hip_knee_vert_dist | 1.17 | > 0.12 → standing |
| left_hip_knee_vert_dist | 1.16 | > 0.12 → standing |
| bbox_aspect_ratio | 0.86 | > 1.55 → standing |
| angle_left_shoulder_hip_knee | 0.85 | > 128° → standing |
| angle_right_shoulder_hip_knee | 0.80 | > 128° → standing |

### 適用結果

**定量評価**

| 指標 | 値 |
|---|---|
| Macro F1 | 0.7172 |
| Sitting Precision/Recall | 0.73 / 0.65 |
| Standing Precision/Recall | 0.71 / 0.78 |

**定性評価**

- sittingの見逃し（sittingをstandingと誤判定）が多い
- bboxアスペクト比の条件分岐にif/elifの記述順バグが存在し、一部のケースで正しく評価されていなかった

### 考察

Cohen's dが最高の特徴量を使った単純なスコアリングでもMacro F1 = 0.72程度を達成できた。一方で、**各特徴量は独立に閾値判定されており、特徴量間の相互作用（例：角度が低くてもbboxが縦長なら高確率でstanding）を捉えられていない**。また、実装バグが発見されたため修正が必要と判断した。

---

## 2.2 v2 — ルールベース改良（バグ修正 + 8特徴量）

### 仮説

v1のバグを修正しつつ、下半身キーポイントの信頼度情報と腰のy座標を追加することで、**キーポイントが隠れているケース（遮蔽）を識別**できると仮説を立てた。座り姿では椅子や机でひざ・足首が隠れるため、信頼度が低いこと自体が「sitting」の証拠になりうる。

### ロジック

v1に以下を追加した：

- バグ修正: bbox_aspect_ratioの条件分岐を正しい順序に修正
- 追加特徴量3点:
  - `mean_conf_lower`: 下半身6キーポイント（腰・膝・足首）の平均信頼度（< 0.45 → sittingスコア+1）
  - `rel_left_hip_y`, `rel_right_hip_y`: bbox内の腰のy相対座標（> 0.75 → sittingスコア+1 / < 0.65 → standingスコア+1）

### 適用結果

**定量評価**

| 指標 | 値 |
|---|---|
| Macro F1 | 0.7300 |
| Sitting Precision/Recall | 0.75 / 0.67 |
| Standing Precision/Recall | 0.72 / 0.79 |

**定性評価**

- v1バグ修正により全体的に安定
- standing → sittingの誤分類（特にstanding姿勢でhipが下に位置するケース）が依然として多い
- 信頼度特徴量の効果は限定的（sitting/standingともに平均信頼度が近い: 0.582 vs 0.595）

### 考察

バグ修正と特徴量追加で+1.3ポイントの改善を確認した。しかし**信頼度特徴量単独の識別力は弱く、他の特徴量との組み合わせが必要**であることが分かった。また、ルールベースでは複数の特徴量の非線形な相互作用（例：「角度は低いが信頼度も低い場合はsitting」）を表現できない限界が明確になった。機械学習モデルへの移行が必要と判断した。

---

## 2.3 v3 — LightGBM + StratifiedKFold(5)（22特徴量）

### 仮説

ルールベースで設計した特徴量をそのまま入力とし、勾配ブースティング（LightGBM）で学習させることで、**特徴量間の非線形相互作用**を自動的に捉えられると仮説を立てた。

また、StratifiedKFoldによる5分割交差検証を用いることで、データが927件と限られる中でも信頼性の高い汎化性能の推定ができると考えた。

### ロジック

**特徴量（22次元）**

| カテゴリ | 特徴量名 | 説明 |
|---|---|---|
| Bbox形状 | `bbox_aspect_ratio` | h/w比（立ち=縦長） |
| hip-knee距離 | `left/right_hip_knee_vert` | 膝-腰垂直距離（bbox正規化） |
| hip-ankle距離 | `left/right_hip_ankle_vert` | 足首-腰垂直距離（bbox正規化） |
| 関節角度 | `angle_l/r_shoulder_hip_knee` | 肩-腰-膝角度（立ち≈145°, 座り≈112°） |
| 関節角度 | `angle_l/r_hip_knee_ankle` | 腰-膝-足首角度（立ち≈129°, 座り≈102°） |
| 腰y座標 | `rel_left/right_hip_y` | 腰のbbox内相対y座標 |
| 膝y座標 | `rel_left/right_knee_y` | 膝のbbox内相対y座標 |
| 下半身信頼度 | `mean/min_conf_lower` | 下半身6KPの平均・最小信頼度 |
| 個別信頼度 | `conf_left/right_knee/ankle` | 膝・足首個別信頼度 |
| 身体比率 | `torso_leg_ratio` | 体幹長/脚長比率 |
| 膝x位置 | `left/right_knee_x_offset` | 膝の腰に対するx方向オフセット（座り時は前方に出る） |

**モデル設定**

```python
LightGBMClassifier(
    n_estimators=300, learning_rate=0.05,
    num_leaves=31, max_depth=6,
    subsample=0.8, colsample_bytree=0.8,
    reg_alpha=0.1, reg_lambda=0.1, seed=42
)
StratifiedKFold(n_splits=5, shuffle=True, random_state=42)
```

### 適用結果

**定量評価**

OOF（Out-of-Fold）による評価結果：

| 指標 | 値 |
|---|---|
| **OOF Macro F1** | **0.7915** |
| Fold間標準偏差 | 0.022（安定） |

| Fold | Macro F1 |
|---|---|
| 0 | ~0.77 |
| 1 | ~0.81 |
| 2 | ~0.79 |
| 3 | ~0.80 |
| 4 | ~0.80 |

**特徴量重要度（gain）上位10**

| 順位 | 特徴量 | Gain |
|---|---|---|
| 1 | `bbox_aspect_ratio` | 511 |
| 2 | `right_knee_x_offset` | 370 |
| 3 | `angle_r_shoulder_hip_knee` | 330 |
| 4 | `left_hip_knee_vert` | ~280 |
| 5 | `angle_l_hip_knee_ankle` | ~250 |

**定性評価**

- sittingとstandingの双方に対してバランスよく機能
- 下半身キーポイントが低信頼度のサンプル（62.6%）に対しても、bbox_aspect_ratioと膝x-offsetが補完的に機能することで精度を維持
- 5-foldアンサンブルによる推論を `sitting_prediction(data)` 関数として実装済み

### 考察

LightGBMはルールベース（v2: 0.73）を6ポイント上回るMacro F1 = 0.7915を達成した。**特徴量重要度の分析から、EDAでCohen's dが最高だったhip-knee垂直距離は角度特徴量との共線性により重要度が中位に落ちており、モデルが特徴量の冗長性を自動的に処理していることが分かった**。

一方で、**下限となりうる誤分類パターン**として以下が想定される：
- 下半身がすべて遮蔽されたsittingサンプル（bbox_aspect_ratioだけに頼るケース）
- 片足を伸ばして座るなど非定型姿勢

---

# 3. 結論

本検証では、姿勢推定による幾何学的特徴量を用いた立ち座り判定アルゴリズムを段階的に開発した。

**主な成果**

- LightGBM（22特徴量, 5-fold OOF）でMacro F1 = **0.7915** を達成
- 最も重要な特徴量はbboxアスペクト比（h/w）。EDAで統計的に最強だったhip-knee垂直距離は重要度が中位（角度特徴量との共線性）であり、モデルが冗長性を適切に処理していることを確認
- ルールベース（0.73）vs LightGBM（0.79）：非線形な特徴量組み合わせの学習が有効

**残課題**

1. **閾値最適化**: 現在は固定閾値0.5。sittingとstandingのリコールバランスによっては最適閾値が異なる可能性
2. **キーポイント遮蔽への対処**: 下半身KP低信頼度サンプルが62.6%存在。遮蔽パターン自体を特徴量化（例: 低信頼度KPの2値フラグ）することで改善の余地あり
3. **特徴量拡張**: 全17キーポイントの相対座標（34次元）を直接入力として追加することで、現在の手作り特徴量では捉えられていない姿勢パターンへの対応が期待できる
4. **モデル比較**: XGBoost・CatBoostとの比較未実施

**今後の推奨アクション**

```
優先度高（堅実案）:
  1. 閾値最適化（Precision/Recall曲線から最適点を選択）
  2. 特徴量追加: 全KP相対座標 + KP間ユークリッド距離
  3. XGBoost / CatBoost との比較

優先度中（爆発案）:
  4. 信頼度スコアのみ（17次元）で分類する単独モデルの検証
     → 遮蔽パターンだけで識別できるかを検証
  5. GNNを用いたスケルトングラフ分類
     → 17ノード、COCOエッジ構造でGNN分類器を構築
```

---

# 4. 参考文献

1. COCO Keypoints format: [https://cocodataset.org/#keypoints-2020](https://cocodataset.org/#keypoints-2020)
2. LightGBM: Ke, G. et al. (2017). "LightGBM: A Highly Efficient Gradient Boosting Decision Tree." *NeurIPS 2017*.
3. Cohen, J. (1988). *Statistical Power Analysis for the Behavioral Sciences* (2nd ed.). — Cohen's d の参照基準（d > 0.8: large effect size）
4. scikit-learn: Pedregosa et al. (2011). "Scikit-learn: Machine Learning in Python." *JMLR 12*, 2825–2830.
